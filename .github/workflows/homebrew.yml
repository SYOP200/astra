name: Homebrew Formula Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  brew-build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          stable: true

      - name: Tap the Astra Shell Homebrew formula
        run: |
          brew untap astra-shell/homebrew-astra-shell 2>/dev/null || true
          brew tap astra-shell/homebrew-astra-shell https://github.com/astra-shell/homebrew-astra-shell

      - name: Install Astra from tap
        env:
          HOMEBREW_NO_REQUIRE_TAP_TRUST: 1
        run: |
          brew install --build-from-source astra-shell

      - name: Verify Astra runtime
        run: |
          astra --version
          astra theme list
